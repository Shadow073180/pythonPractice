

# replace a string 
def replace_string_in_statement(statement,original_string,replacement_string):
    print(statement.replace(original_string,replacement_string))

#can a string be replaced twice
def can_a_string_be_replaced_twice(statement,original_string,replacement_string):
    print(statement.replace(original_string,replacement_string,))

# can we replace a phrase
def can_we_replace_phrase(statement,original_phrase,replacement_phrase):
    print(statement.replace(original_phrase,replacement_phrase,))